package pageObject.components.MyAccountSideBar;

import pageObject.BasePage;
import org.openqa.selenium.WebDriver;

public class BackInStockSubscriptionsComponent extends BasePage {
    WebDriver driver;

    public BackInStockSubscriptionsComponent(WebDriver driver) {
        super(driver);
    }
}
